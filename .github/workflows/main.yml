name: Build RPM Package

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    
    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y rpm gcc make

    - name: Create source tarball
      run: |
        tar czf clevo-keyboard-backlight-1.0.tar.gz --transform 's,^,clevo-keyboard-backlight-1.0/,' *

    - name: Build RPM package
      run: |
        rpmbuild -ba clevo-keyboard-backlight.spec --define "_sourcedir $PWD" --define "_rpmdir $PWD"

    - name: List built RPMs
      run: find . -name "*.rpm"

    - name: Upload RPM
      uses: actions/upload-artifact@v2
      with:
        name: clevo-keyboard-rpm
        path: ./*.rpm
